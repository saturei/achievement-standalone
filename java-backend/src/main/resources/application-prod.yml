spring:
  datasource:
    url: jdbc:mysql://localhost:3306/achievement_db_prod?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root

logging:
  level:
    com.example.achievement: INFO
